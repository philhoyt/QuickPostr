# Node
node_modules/

# Build artifacts — generated by CI on release, not committed
build/
*.map

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Environment
.env
.env.*

# PHP/Composer — root dev dependencies only; the vendored copy under
# lib/plugin-update-checker/vendor/ (Parsedown) ships in the release.
/vendor/

# WordPress
wp-content/

# Project docs (internal planning only)
/docs
*.zip

# Implementation plans — local working notes, not versioned
/plans

# Claude Code — machine-specific session permissions, not shared
.claude/settings.local.json

# PHPUnit
.phpunit.result.cache
